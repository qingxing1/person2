# Person Monorepo 项目启动指南

欢迎使用 Person Monorepo 项目！本项目采用 monorepo 架构，整合了后端服务、管理后台和前端网站三个应用。

## 项目结构

```
person-monorepo/
├── packages/
│   ├── backend/          # NestJS 后端服务
│   ├── admin-frontend/   # Vue 3 管理后台
│   ├── personal-site/    # React 个人网站
│   └── shared/           # 共享代码
│       ├── types/        # 共享类型定义
│       └── utils/        # 共享工具函数
├── pnpm-workspace.yaml   # PNPM 工作区配置
└── package.json          # 根目录配置
```

## 环境要求

- Node.js >= 18.0.0
- pnpm >= 8.0.0

## 安装依赖

首次运行项目前，请先安装所有依赖：

```bash
pnpm install
```

## 启动项目

### 1. 启动单个项目

#### 启动后端服务
```bash
# 进入后端目录
cd packages/backend

# 安装依赖（如果尚未安装）
pnpm install

# 启动开发服务器
NODE_ENV=development pnpm start:dev
```

后端服务将在 `http://localhost:6999` 启动。

注意：后端服务需要以下依赖：
- MySQL 数据库（配置在 `packages/backend/src/config/dev.yml` 中）
- Redis 服务
- 上传目录（项目根目录下的 `upload` 文件夹，已自动创建）

#### 启动管理后台
```bash
# 进入管理后台目录
cd packages/admin-frontend

# 安装依赖（如果尚未安装）
pnpm install

# 启动开发服务器
pnpm dev
```

管理后台将在 `http://localhost:5173` 启动。

#### 启动个人网站
```bash
# 进入个人网站目录
cd packages/personal-site

# 安装依赖（如果尚未安装）
pnpm install

# 启动开发服务器
pnpm dev
```

个人网站将在 `http://localhost:3000` 启动。

### 2. 使用根目录脚本启动

#### 启动所有项目（并行）
```bash
# 启动所有开发服务器
pnpm dev
```

此命令会使用 `concurrently` 同时启动所有项目的开发服务器。

#### 单独启动某个项目
```bash
# 启动后端服务
pnpm dev:backend

# 启动管理后台
pnpm dev:admin

# 启动个人网站
pnpm dev:personal
```

## 构建项目

### 构建所有项目
```bash
pnpm build
```

### 构建单个项目
```bash
# 构建后端
cd packages/backend && pnpm build

# 构建管理后台
cd packages/admin-frontend && pnpm build

# 构建个人网站
cd packages/personal-site && pnpm build
```

## 共享包使用

本项目包含两个共享包，可在各个应用中使用：

### 共享类型 (@person/shared-types)
```typescript
import { Blog } from '@person/shared-types';

const blog: Blog = {
  id: '1',
  title: 'Sample Blog',
  // ...
};
```

### 共享工具 (@person/shared-utils)
```typescript
import { formatDate, deepClone } from '@person/shared-utils';

const formattedDate = formatDate(new Date());
const clonedObject = deepClone(originalObject);
```

## 数据库配置

后端服务需要连接数据库，请确保：

1. MySQL 服务正在运行
2. 在 `packages/backend/src/config/` 中配置正确的数据库连接信息
3. 数据库已创建并初始化（参考 `kapok.sql` 文件）

## Redis 配置

后端服务使用 Redis 进行缓存，请确保：

1. Redis 服务正在运行
2. 在配置文件中设置正确的 Redis 连接信息

## 常见问题

### 1. 依赖安装问题
如果遇到依赖安装问题，请尝试：
```bash
# 清理缓存
pnpm store prune

# 重新安装
pnpm install
```

### 2. 端口冲突
如果默认端口被占用，可以在各项目的 `.env` 文件中修改端口配置。

### 3. 共享包更新
当更新共享包后，需要重新构建：
```bash
cd packages/shared/types && pnpm build
cd packages/shared/utils && pnpm build
```

## 开发建议

1. 在进行跨包修改时，建议使用根目录的开发命令以确保所有服务同步运行
2. 共享包的修改会直接影响所有引用它的项目，请谨慎修改
3. 使用 Git 进行版本控制，建议在 monorepo 根目录进行所有 Git 操作

## 项目维护

- 后端服务：NestJS + TypeORM + MySQL + Redis
- 管理后台：Vue 3 + TypeScript + Element Plus
- 个人网站：React + TypeScript + Tailwind CSS

享受您的开发体验！